name: Keep Alive
on:
  schedule:
    # This runs every 4 hours
    - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the app
        run: |
          curl -I https://stormbot.streamlit.app